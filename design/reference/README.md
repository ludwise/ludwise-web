# Reference designs

## `screens.md`

The four screens of the LUDWISE consumer web product as composed from the design
system: what goes on each screen, in what order, at what density, and which
states each demonstrates. Each screen's source is included in a fenced block.

Sales · Search results · Game detail · Preferences.

The sample data at the end is illustrative. **Do not treat it as a schema** — the
canonical domain model does not exist yet (see `ROADMAP.md` → *Next — the domain
model*).

## `logo-exploration.html`

The six logo directions considered and why the adopted one won. Opens standalone
in a browser. Sketches in code, not finished artwork.
