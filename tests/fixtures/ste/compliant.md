# Compliant fixture

This paragraph obeys the profile. Each sentence is short. The words are plain.

## A procedure

1. Open the policy file.
2. Change the rollout mode to the enforcement value.
3. Run the language check again.

## A description

The checker reads one file at a time. It classifies the path first. It then
extracts the prose units. Each unit passes through the deterministic rules.
